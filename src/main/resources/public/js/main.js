var app = angular.module("springApp", []);

app.controller("AppCtrl", function ($scope) {
    $scope.websites = [{
        iconImageUrl:' ',
        id:'myID',
        website:'pornhub.com',
        title:'milf',
        description:'skip it'
    }];
});
